name: superintendent bootstrap

on: [workflow_dispatch]

jobs:
  smoke:
    runs-on: windows-latest
    steps:
      - name: smoke test 1s
        run: |
          echo "Bootstrap OK (no UI launch on CI)"
