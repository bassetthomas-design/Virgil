# DEV NOTES — fix/ui-black-window

Objectif branche : corriger la fenêtre noire WPF (MainWindow) et brancher un ViewModel de base.

Tâches :
1) MainWindow.xaml : layout minimal visible + zones placeholders (top bar / chat / actions / statusbar).
2) MainWindow.xaml.cs : DataContext = MainViewModel.
3) MainViewModel.cs : propriétés basiques (Messages, Input, CpuUsage, etc.).
4) Préparation modules Reco IA + Auto-maintenance (squelettes).

Etat : en cours.
