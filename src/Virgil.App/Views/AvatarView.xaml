<UserControl x:Class="Virgil.App.Views.AvatarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:conv="clr-namespace:Virgil.App.Converters"
             mc:Ignorable="d" Height="160" Width="160">
    <UserControl.Resources>
        <conv:MoodToBrushConverter x:Key="MoodBrush"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RenderTransform>
            <ScaleTransform x:Name="Pulse" CenterX="80" CenterY="80" ScaleX="1" ScaleY="1"/>
        </Grid.RenderTransform>
        <Ellipse Width="150" Height="150" Fill="{Binding Mood, Converter={StaticResource MoodBrush}}" Opacity="0.2"/>
        <Ellipse Width="130" Height="130" StrokeThickness="3" Stroke="{Binding Mood, Converter={StaticResource MoodBrush}}"/>
        <Grid>
            <Ellipse Width="24" Height="24" Fill="{Binding Mood, Converter={StaticResource MoodBrush}}" Margin="-30,0,0,0" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <Ellipse Width="24" Height="24" Fill="{Binding Mood, Converter={StaticResource MoodBrush}}" Margin="30,0,0,0" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</UserControl>
