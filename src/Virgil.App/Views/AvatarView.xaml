<UserControl x:Class="Virgil.App.Views.AvatarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:conv="clr-namespace:Virgil.App.Converters"
             mc:Ignorable="d" Height="180" Width="180">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Virgil.App;component/Themes/AvatarTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <conv:MoodToBrushConverter x:Key="MoodBrush"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RenderTransform>
            <ScaleTransform x:Name="Pulse" CenterX="90" CenterY="90"/>
        </Grid.RenderTransform>

        <!-- Core fill (subtle) -->
        <Ellipse Width="160" Height="160" Fill="{StaticResource VirgilCoreFill}"/>

        <!-- Rings -->
        <Ellipse Width="168" Height="168" Style="{StaticResource OuterRingStyle}"/>
        <Ellipse Width="150" Height="150" Style="{StaticResource InnerRingStyle}"/>

        <!-- Eyes -->
        <Grid>
            <Ellipse Style="{StaticResource EyeStyle}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="-34,0,0,0"/>
            <Ellipse Style="{StaticResource EyeStyle}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="34,0,0,0"/>
        </Grid>

        <!-- Mood tint overlay -->
        <Ellipse Width="168" Height="168" StrokeThickness="0" Opacity="0.25" Fill="{Binding Mood, Converter={StaticResource MoodBrush}}"/>
    </Grid>
</UserControl>
