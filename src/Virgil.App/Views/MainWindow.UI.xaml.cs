using System.Windows;

namespace Virgil.App
{
    // Laisser ce partiel SANS constructeur pour éviter CS0111.
    // Le constructeur unique est dans MainWindow.xaml.cs.
    public partial class MainWindow : Window
    {
        // Intentionally left empty.
        // Place optional UI-only helpers here if needed (no ctor).
    }
}
