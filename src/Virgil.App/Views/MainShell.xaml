<Window x:Class="Virgil.App.Views.MainShell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:Virgil.App.Views"
        mc:Ignorable="d"
        Title="Virgil — Chat + Monitoring" Height="700" Width="480">
    <DockPanel Background="#0f0f12">
        <views:MetricsBar x:Name="Metrics" DockPanel.Dock="Bottom"/>
        <StackPanel>
            <views:AvatarView DataContext="{Binding ElementName=Metrics, Path=DataContext}" Margin="0,12,0,8" HorizontalAlignment="Center"/>
            <views:ChatView x:Name="Chat"/>
        </StackPanel>
    </DockPanel>
</Window>
