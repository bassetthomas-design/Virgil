<Window x:Class="Virgil.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:Virgil.App.Controls"
        mc:Ignorable="d"
        Title="Virgil Dashboard" Height="600" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Text="Virgil Dashboard"
                   FontSize="24"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,10"/>

        <!-- Buttons -->
        <WrapPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,0,0,10">
            <Button x:Name="CleanButton" Content="Scan &amp; Clean" Width="120" Height="40" Margin="5" Click="CleanButton_Click"/>
            <Button x:Name="AppUpdateButton" Content="Update Apps" Width="120" Height="40" Margin="5" Click="AppUpdateButton_Click"/>
            <Button x:Name="DriverUpdateButton" Content="Update Drivers" Width="120" Height="40" Margin="5" Click="DriverUpdateButton_Click"/>
            <Button x:Name="BrowserCleanButton" Content="Clean Browsers" Width="120" Height="40" Margin="5" Click="BrowserCleanButton_Click"/>
            <Button x:Name="WindowsUpdateButton" Content="Update Windows" Width="120" Height="40" Margin="5" Click="WindowsUpdateButton_Click"/>
            <Button x:Name="StartupButton" Content="Startup Programs" Width="120" Height="40" Margin="5" Click="StartupButton_Click"/>
            <Button x:Name="ProcessesButton" Content="Processes" Width="120" Height="40" Margin="5" Click="ProcessesButton_Click"/>
            <Button x:Name="ServicesButton" Content="Services" Width="120" Height="40" Margin="5" Click="ServicesButton_Click"/>
            <Button x:Name="MaintenanceButton" Content="Run Maintenance" Width="120" Height="40" Margin="5" Click="MaintenanceButton_Click"/>
            <Button x:Name="MonitorButton" Content="Start Monitoring" Width="120" Height="40" Margin="5" Click="MonitorButton_Click"/>
        </WrapPanel>

        <!-- Logs -->
        <TextBox x:Name="OutputBox"
                 Grid.Row="2"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 TextWrapping="Wrap"
                 Margin="0,0,0,10"/>

        <!-- Avatar -->
        <controls:VirgilAvatar x:Name="AvatarControl"
                               Grid.Row="3"
                               HorizontalAlignment="Center"
                               Margin="0,10,0,0"/>
    </Grid>
</Window>
