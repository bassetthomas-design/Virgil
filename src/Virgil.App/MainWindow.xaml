<Window x:Class="Virgil.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Virgil.App"
        xmlns:controls="clr-namespace:Virgil.App.Controls"
        mc:Ignorable="d"
        Title="Virgil" Height="650" Width="960"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,12">
            <TextBlock Text="Virgil — Tableau de bord" FontSize="22" FontWeight="Bold" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right" Spacing="8">
                <controls:VirgilAvatar x:Name="AvatarControl" Width="48" Height="48" />
            </StackPanel>
        </DockPanel>

        <!-- Body -->
        <Grid Grid.Row="1" ColumnSpacing="12" RowSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Actions -->
            <Border Grid.Column="0" Padding="12" CornerRadius="10" Background="#0F000000">
                <StackPanel Spacing="8">
                    <TextBlock Text="Actions rapides" FontWeight="Bold" FontSize="16" Margin="0,0,0,6"/>
                    <Button Content="Scan &amp; Clean" Click="CleanButton_Click" />
                    <Button Content="Check Updates (apps/jeux)" Click="UpdateButton_Click" />
                    <Button x:Name="DriverButton" Content="Check Driver Updates (pilotes)" Click="DriverButton_Click"/>
                    <Button x:Name="MonitorButton" Content="Start Monitoring" Click="MonitorButton_Click"/>
                    <Separator/>
                    <Button x:Name="StartupButton" Content="Basculer: Lancer Virgil au démarrage" Click="StartupButton_Click"/>
                    <Button x:Name="ProcessesButton" Content="Lister processus (top RAM)" Click="ProcessesButton_Click"/>
                    <Separator/>
                    <Button x:Name="WindowsUpdateButton" Content="Windows Update (scan)" Click="WindowsUpdateButton_Click"/>
                </StackPanel>
            </Border>

            <!-- Output -->
            <Border Grid.Column="1" Padding="12" CornerRadius="10" Background="#09000000">
                <Grid>
                    <TextBox x:Name="OutputBox"
                             FontFamily="Consolas"
                             FontSize="13"
                             AcceptsReturn="True"
                             AcceptsTab="True"
                             TextWrapping="NoWrap"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             IsReadOnly="True"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
