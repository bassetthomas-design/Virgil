<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Virgil.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:Virgil.App"
    xmlns:controls="clr-namespace:Virgil.App.Controls"
    mc:Ignorable="d"
    Title="Virgil" Height="700" Width="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="16,12">
            <TextBlock Text="Virgil — Assistant Système Intelligent" FontSize="18" FontWeight="Bold"/>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" >
                <Button Content="Maintenance complète" Margin="8,0"/>
                <Button Content="Nettoyage intelligent" Margin="8,0"/>
                <Button Content="Nettoyer navigateurs" Margin="8,0"/>
                <Button Content="Tout mettre à jour" Margin="8,0"/>
                <Button Content="Defender (MAJ + Scan)" Margin="8,0"/>
                <Button Content="Ouvrir configuration" Margin="8,0"/>
            </StackPanel>
        </DockPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="340"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel: Avatar + Stats placeholders -->
            <StackPanel Grid.Column="0" Margin="16,0,8,0">
                <controls:AvatarControl Height="220"/>

                <GroupBox Header="Système" Margin="0,12,0,0">
                    <StackPanel>
                        <TextBlock Text="CPU / GPU / RAM / Disque — à brancher"/>
                    </StackPanel>
                </GroupBox>
            </StackPanel>

            <!-- Right Panel: Chat -->
            <Grid Grid.Column="1" Margin="8,0,16,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Border BorderBrush="#333" BorderThickness="1" CornerRadius="8" Padding="12">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="ChatItems"/>
                    </ScrollViewer>
                </Border>
                <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,8,0,0">
                    <TextBox x:Name="UserInput" Width="Auto" MinWidth="400"/>
                    <Button Content="Envoyer" Margin="8,0,0,0"/>
                </StackPanel>
            </Grid>
        </Grid>

        <!-- Footer / Status bar -->
        <DockPanel Grid.Row="2" Margin="16,8">
            <TextBlock Text="Statut: prêt"/>
            <TextBlock DockPanel.Dock="Right" Text="00:00:00"/>
        </DockPanel>
    </Grid>
</Window>
