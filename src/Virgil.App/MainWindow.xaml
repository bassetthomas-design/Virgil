<Window x:Class="Virgil.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Virgil.App.Controls"
        Title="Virgil" Height="680" Width="1080" MinHeight="560" MinWidth="900" Background="#0E0E10">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="28"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="True" Margin="12,8,12,8">
            <TextBlock Text="Virgil" FontWeight="Bold" Foreground="#E6E6E6" VerticalAlignment="Center"/>
            <TextBlock Text="|" Margin="8,0" Foreground="#8A8A8A" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding HeaderStatus}" Foreground="#CFCFCF" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding Now}" Foreground="#CFCFCF" HorizontalAlignment="Right" DockPanel.Dock="Right"/>
        </DockPanel>

        <Grid Grid.Row="1" Margin="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <Border Background="#141418" CornerRadius="8" Padding="14" Margin="0,0,10,0">
                <StackPanel>
                    <local:AvatarControl Height="180"/>
                    <Separator/>
                    <TextBlock Text="{Binding CpuUsage,StringFormat=Cpu {0:F0}%}" Foreground="#E6E6E6"/>
                    <TextBlock Text="{Binding RamUsage,StringFormat=Ram {0:F0}%}" Foreground="#E6E6E6"/>
                    <TextBlock Text="{Binding GpuUsage,StringFormat=Gpu {0:F0}%}" Foreground="#E6E6E6"/>
                    <TextBlock Text="{Binding CpuTemp,StringFormat=Temp {0:F0}°C}" Foreground="#E6E6E6"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="#121216" CornerRadius="8" Padding="14">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Messages}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#1B1B22" CornerRadius="6" Padding="10" Margin="0,6">
                                    <TextBlock Text="{Binding}" TextWrapping="Wrap" Foreground="#EDEDED"/>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>

            <Border Grid.Column="2" Background="#141418" CornerRadius="8" Padding="14" Margin="10,0,0,0">
                <StackPanel>
                    <TextBlock Text="Actions" FontWeight="SemiBold" Foreground="#E6E6E6"/>
                    <Separator/>
                    <Button Content="Surveillance ON/OFF" Margin="0,6"/>
                    <Button Content="Maintenance complète" Margin="0,6"/>
                    <Button Content="Nettoyage intelligent" Margin="0,6"/>
                    <Button Content="Tout mettre à jour" Margin="0,6"/>
                    <Button Content="Defender (MAJ + Scan)" Margin="0,6"/>
                    <Button Content="Ouvrir configuration" Margin="0,6"/>
                </StackPanel>
            </Border>
        </Grid>

        <StatusBar Grid.Row="2" Background="#101015">
            <StatusBarItem>
                <TextBlock Text="{Binding FooterStatus}" Foreground="#CFCFCF"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>