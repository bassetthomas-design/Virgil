<Window x:Class="Virgil.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:Virgil.App.Controls"
        mc:Ignorable="d"
        Title="Virgil" Height="720" Width="1024"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,12">
            <TextBlock Text="Virgil — Tableau de bord" FontSize="22" FontWeight="Bold" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                <controls:VirgilAvatar x:Name="AvatarControl" Width="48" Height="48" Margin="8,0,0,0"/>
            </StackPanel>
        </DockPanel>

        <!-- Body -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Actions -->
            <Border Grid.Column="0" Padding="12" CornerRadius="10" Background="#0F000000" Margin="0,0,12,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Actions rapides" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>

                        <!-- Presets -->
                        <Button Content="Maintenance rapide (Temp + Navigateurs)" Click="QuickMaintenanceButton_Click" Margin="0,0,0,8"/>
                        <Button Content="Maintenance complète (Clean + MAJ + WU)" Click="FullMaintenanceButton_Click" Margin="0,0,0,12"/>

                        <!-- Nettoyage -->
                        <TextBlock Text="Nettoyage" FontWeight="Bold" Margin="0,0,0,6"/>
                        <Button Content="Scan &amp; Clean (Temp)" Click="CleanButton_Click" Margin="0,0,0,8"/>
                        <Button x:Name="CleanBrowsersButton" Content="Clean Browsers (caches)" Click="CleanBrowsersButton_Click" Margin="0,0,0,8"/>
                        <Button Content="Clean Extended (Jeux/Adobe/NVIDIA…)" Click="CleanExtendedButton_Click" Margin="0,0,0,12"/>

                        <!-- Mises à jour -->
                        <TextBlock Text="Mises à jour" FontWeight="Bold" Margin="0,0,0,6"/>
                        <Button Content="Check Updates (apps/jeux via winget)" Click="UpdateButton_Click" Margin="0,0,0,8"/>
                        <Button x:Name="DriverButton" Content="Check Driver Updates (winget)" Click="DriverButton_Click" Margin="0,0,0,12"/>

                        <!-- Surveillance -->
                        <TextBlock Text="Surveillance" FontWeight="Bold" Margin="0,0,0,6"/>
                        <Button x:Name="MonitorButton" Content="Start Monitoring" Click="MonitorButton_Click" Margin="0,0,0,8"/>
                        <Button Content="Lire températures (CPU/GPU/Disque)" Click="ReadTempsButton_Click" Margin="0,0,0,12"/>

                        <Separator Margin="0,8,0,8"/>

                        <!-- Démarrage &amp; Processus -->
                        <TextBlock Text="Démarrage &amp; Processus" FontWeight="Bold" Margin="0,0,0,6"/>
                        <Button x:Name="StartupButton" Content="Basculer: Lancer Virgil au démarrage" Click="StartupButton_Click" Margin="0,0,0,8"/>
                        <Button x:Name="ProcessesButton" Content="Lister processus (top RAM)" Click="ProcessesButton_Click" Margin="0,0,0,6"/>
                        <StackPanel Orientation="Horizontal" Margin="0,6,0,12">
                            <TextBox x:Name="KillPidBox" Width="120" ToolTip="PID à tuer" Margin="0,0,8,0"/>
                            <Button Content="Kill PID" Click="KillPidButton_Click"/>
                        </StackPanel>

                        <Separator Margin="0,8,0,8"/>

                        <!-- Windows Update -->
                        <TextBlock Text="Windows Update" FontWeight="Bold" Margin="0,0,0,6"/>
                        <Button x:Name="WindowsUpdateButton" Content="WU: Scan" Click="WindowsUpdateButton_Click" Margin="0,0,0,6"/>
                        <Button Content="WU: Download" Click="WindowsUpdateDownloadButton_Click" Margin="0,0,0,6"/>
                        <Button Content="WU: Install" Click="WindowsUpdateInstallButton_Click" Margin="0,0,0,6"/>
                        <Button Content="WU: Restart (si requis)" Click="WindowsUpdateRestartButton_Click" Margin="0,0,0,12"/>

                        <Separator Margin="0,8,0,8"/>

                        <!-- Services Windows -->
                        <TextBlock Text="Services Windows" FontWeight="Bold" Margin="0,0,0,6"/>
                        <Button Content="Lister services" Click="ListServicesButton_Click" Margin="0,0,0,6"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBox x:Name="SvcNameBox" Width="180" ToolTip="Nom du service" Margin="0,0,8,0"/>
                            <Button Content="Restart" Click="RestartServiceButton_Click" Margin="0,0,8,0"/>
                            <Button Content="Start" Click="StartServiceButton_Click" Margin="0,0,8,0"/>
                            <Button Content="Stop" Click="StopServiceButton_Click"/>
                        </StackPanel>

                        <Separator Margin="0,8,0,8"/>

                        <!-- Config -->
                        <TextBlock Text="Configuration" FontWeight="Bold" Margin="0,0,0,6"/>
                        <Button Content="Afficher config" Click="ShowConfigButton_Click" Margin="0,0,0,6"/>
                        <Button Content="Sauver config (utilisateur)" Click="SaveConfigButton_Click" Margin="0,0,0,6"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- Output -->
            <Border Grid.Column="1" Padding="12" CornerRadius="10" Background="#09000000">
                <Grid>
                    <TextBox x:Name="OutputBox"
                             FontFamily="Consolas"
                             FontSize="13"
                             AcceptsReturn="True"
                             AcceptsTab="True"
                             TextWrapping="NoWrap"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             IsReadOnly="True"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
