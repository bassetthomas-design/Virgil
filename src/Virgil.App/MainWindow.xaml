<Window x:Class="Virgil.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:Virgil.App.Controls"
        mc:Ignorable="d"
        Title="Virgil" Height="600" Width="960" MinHeight="480" MinWidth="720">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="28"/>
        </Grid.RowDefinitions>

        <!-- Barre du haut -->
        <DockPanel Grid.Row="0" LastChildFill="True" Margin="8,6,8,6">
            <TextBlock Text="Virgil" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock Text="|" Margin="8,0" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding HeaderStatus}" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding Now}" HorizontalAlignment="Right" DockPanel.Dock="Right"/>
        </DockPanel>

        <!-- Zone principale -->
        <Grid Grid.Row="1" Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="260"/>
            </Grid.ColumnDefinitions>

            <!-- Panneau gauche (avatar + jauges) -->
            <Border Background="#111" CornerRadius="6" Padding="12" Margin="0,0,8,0">
                <StackPanel>
                    <controls:AvatarView Source="{Binding AvatarPath}" Width="192" Height="192" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                    <TextBlock Text="{Binding CurrentMood}" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                    <Separator/>
                    <TextBlock Text="CPU:{Binding CpuUsage}%"/>
                    <TextBlock Text="RAM:{Binding RamUsage}%"/>
                    <TextBlock Text="GPU:{Binding GpuUsage}%"/>
                    <TextBlock Text="Temp:{Binding CpuTemp}°C"/>
                    <Button Content="Changer d’humeur" Command="{Binding MoodCycleCommand}" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <!-- Chat centre -->
            <Border Grid.Column="1" Background="#151515" CornerRadius="6" Padding="12">
                <DockPanel>
                    <ScrollViewer VerticalScrollBarVisibility="Auto" DockPanel.Dock="Top" Height="Auto">
                        <ItemsControl ItemsSource="{Binding Messages}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="0,2"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                    <DockPanel LastChildFill="True" Margin="0,8,0,0" DockPanel.Dock="Bottom">
                        <TextBox Text="{Binding Input, UpdateSourceTrigger=PropertyChanged}" Height="28"/>
                        <Button Content="Envoyer" Command="{Binding SendCommand}" Width="96" Margin="8,0,0,0"/>
                    </DockPanel>
                </DockPanel>
            </Border>

            <!-- Panneau droit (actions rapides) -->
            <Border Grid.Column="2" Background="#111" CornerRadius="6" Padding="12" Margin="8,0,0,0">
                <StackPanel>
                    <TextBlock Text="Actions" FontWeight="SemiBold"/>
                    <Separator/>
                    <Button Content="Surveillance ON/OFF" Margin="0,4"/>
                    <Button Content="Maintenance complète" Margin="0,4"/>
                    <Button Content="Nettoyage intelligent" Margin="0,4"/>
                    <Button Content="Tout mettre à jour" Margin="0,4"/>
                    <Button Content="Defender (MAJ + Scan)" Margin="0,4"/>
                    <Button Content="Ouvrir configuration" Margin="0,4"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Barre d’état bas -->
        <StatusBar Grid.Row="2">
            <StatusBarItem Content="{Binding FooterStatus}"/>
        </StatusBar>
    </Grid>
</Window>
