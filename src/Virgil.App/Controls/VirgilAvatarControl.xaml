<UserControl x:Class="Virgil.App.Controls.VirgilAvatarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="240"
             d:DesignWidth="240">
    <UserControl.Resources>
        <SolidColorBrush x:Key="AccentBrush" Color="#4CAF50" />
        <SolidColorBrush x:Key="AccentStrokeBrush" Color="#804CAF50" />
        <SolidColorBrush x:Key="FaceFillBrush" Color="#0F181A" />
        <RadialGradientBrush x:Key="EyeFillBrush"
                             Center="0.45,0.35"
                             GradientOrigin="0.42,0.32"
                             RadiusX="0.9"
                             RadiusY="0.9">
            <GradientStop Color="#F6FFFF" Offset="0" />
            <GradientStop Color="#D9EFFB" Offset="0.55" />
            <GradientStop Color="#ABC8DA" Offset="1" />
        </RadialGradientBrush>
        <SolidColorBrush x:Key="EyeHighlightBrush" Color="#CCFFFFFF" />
    </UserControl.Resources>

    <Grid x:Name="Root" HorizontalAlignment="Center" VerticalAlignment="Center">
        <Viewbox Stretch="Uniform">
            <Grid Width="220" Height="220">
                <Ellipse Fill="{StaticResource FaceFillBrush}"
                         Stroke="{StaticResource AccentStrokeBrush}"
                         StrokeThickness="6"
                         x:Name="FaceEllipse">
                    <Ellipse.Effect>
                        <DropShadowEffect x:Name="FaceGlow"
                                          Color="#884CAF50"
                                          BlurRadius="24"
                                          ShadowDepth="0"
                                          Opacity="0.8" />
                    </Ellipse.Effect>
                </Ellipse>

                <Ellipse Stroke="{StaticResource AccentBrush}"
                         StrokeThickness="5"
                         Width="220"
                         Height="220"
                         x:Name="AccentRing">
                    <Ellipse.Effect>
                        <DropShadowEffect x:Name="RingGlow"
                                          Color="#884CAF50"
                                          BlurRadius="16"
                                          ShadowDepth="0"
                                          Opacity="0.6" />
                    </Ellipse.Effect>
                </Ellipse>

                <Grid x:Name="EyesLayer"
                      Width="130"
                      Height="56"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center">
                    <Grid.RenderTransform>
                        <TransformGroup>
                            <TranslateTransform x:Name="EyesOffset" />
                        </TransformGroup>
                    </Grid.RenderTransform>

                    <Grid x:Name="LeftEyeContainer"
                          RenderTransformOrigin="0.5,0.5"
                          Width="32"
                          Height="32"
                          HorizontalAlignment="Left"
                          Margin="12,0,0,0">
                        <Grid.RenderTransform>
                            <TransformGroup>
                                <ScaleTransform x:Name="LeftEyeScale" />
                                <TranslateTransform x:Name="LeftEyeOffset" X="-8" />
                            </TransformGroup>
                        </Grid.RenderTransform>
                        <Grid.Clip>
                            <EllipseGeometry Center="16,16"
                                             RadiusX="16"
                                             RadiusY="16" />
                        </Grid.Clip>
                        <Ellipse x:Name="LeftEye"
                                 Fill="{StaticResource EyeFillBrush}"
                                 Stroke="#66FFFFFF"
                                 StrokeThickness="1.1">
                            <Ellipse.Effect>
                                <DropShadowEffect BlurRadius="12"
                                                  ShadowDepth="0"
                                                  Opacity="0.3"
                                                  Color="{Binding Source={StaticResource AccentBrush}, Path=Color}" />
                            </Ellipse.Effect>
                        </Ellipse>
                        <Ellipse Width="32"
                                 Height="32"
                                 Stroke="{StaticResource AccentBrush}"
                                 StrokeThickness="1.4"
                                 Opacity="0.18" />
                        <Ellipse Width="18"
                                 Height="18"
                                 Fill="{StaticResource AccentBrush}"
                                 Opacity="0.14"
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Center" />
                        <Ellipse Width="7"
                                 Height="7"
                                 Fill="{StaticResource EyeHighlightBrush}"
                                 Opacity="0.55"
                                 HorizontalAlignment="Left"
                                 VerticalAlignment="Top"
                                 Margin="6,6,0,0" />
                    </Grid>

                    <Grid x:Name="RightEyeContainer"
                          RenderTransformOrigin="0.5,0.5"
                          Width="32"
                          Height="32"
                          HorizontalAlignment="Right"
                          Margin="0,0,12,0">
                        <Grid.RenderTransform>
                            <TransformGroup>
                                <ScaleTransform x:Name="RightEyeScale" />
                                <TranslateTransform x:Name="RightEyeOffset" X="8" />
                            </TransformGroup>
                        </Grid.RenderTransform>
                        <Grid.Clip>
                            <EllipseGeometry Center="16,16"
                                             RadiusX="16"
                                             RadiusY="16" />
                        </Grid.Clip>
                        <Ellipse x:Name="RightEye"
                                 Fill="{StaticResource EyeFillBrush}"
                                 Stroke="#66FFFFFF"
                                 StrokeThickness="1.1">
                            <Ellipse.Effect>
                                <DropShadowEffect BlurRadius="12"
                                                  ShadowDepth="0"
                                                  Opacity="0.3"
                                                  Color="{Binding Source={StaticResource AccentBrush}, Path=Color}" />
                            </Ellipse.Effect>
                        </Ellipse>
                        <Ellipse Width="32"
                                 Height="32"
                                 Stroke="{StaticResource AccentBrush}"
                                 StrokeThickness="1.4"
                                 Opacity="0.18" />
                        <Ellipse Width="18"
                                 Height="18"
                                 Fill="{StaticResource AccentBrush}"
                                 Opacity="0.14"
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Center" />
                        <Ellipse Width="7"
                                 Height="7"
                                 Fill="{StaticResource EyeHighlightBrush}"
                                 Opacity="0.55"
                                 HorizontalAlignment="Left"
                                 VerticalAlignment="Top"
                                 Margin="6,6,0,0" />
                    </Grid>
                </Grid>
            </Grid>
        </Viewbox>
    </Grid>
</UserControl>
