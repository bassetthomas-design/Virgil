<UserControl x:Class="Virgil.App.Controls.VirgilChatPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Virgil.App.Controls"
             xmlns:app="clr-namespace:Virgil.App"
             Background="Transparent">
    <Grid>
        <!-- poussière pour l’effet thanos -->
        <Canvas x:Name="DustLayer" IsHitTestVisible="False"/>

        <ScrollViewer x:Name="Scroller" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="MessageList" ItemsSource="{Binding Messages, RelativeSource={RelativeSource AncestorType=local:VirgilChatPanel}}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type app:ChatMessage}">
                        <Border x:Name="Bubble"
                                CornerRadius="10"
                                Background="{Binding BubbleBrush}"
                                Padding="10" Margin="2,6"
                                SnapsToDevicePixels="True"
                                Opacity="1">
                            <TextBlock Text="{Binding Text}" Foreground="White" TextWrapping="Wrap"
                                       FontSize="14"/>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
