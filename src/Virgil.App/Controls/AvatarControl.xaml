<UserControl x:Class="Virgil.App.Controls.AvatarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="220" d:DesignWidth="220">
    <Grid>
        <!-- Image de base de l’avatar -->
        <Image x:Name="AvatarImage"
               Stretch="Uniform"
               Source="/Assets/avatar/base.png"/>

        <!-- Humeur en overlay (couleur/élément graphique) -->
        <Border x:Name="MoodOverlay"
                Background="{Binding MoodOverlayBrush, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Opacity="0.18"/>

        <!-- Glyph d’humeur -->
        <Image x:Name="MoodGlyphImage"
               Width="48" Height="48"
               HorizontalAlignment="Right" VerticalAlignment="Top"
               Margin="8"
               Source="{Binding MoodGlyph, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
    </Grid>
</UserControl>
