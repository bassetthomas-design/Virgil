<UserControl x:Class="Virgil.App.Controls.AvatarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignWidth="280" d:DesignHeight="180">
    <Grid>
        <Grid.Resources>
            <Storyboard x:Key="Blink">
                <DoubleAnimation Storyboard.TargetName="Lid" Storyboard.TargetProperty="Height" From="0" To="18" Duration="0:0:0.08" AutoReverse="True"/>
            </Storyboard>
        </Grid.Resources>
        <Ellipse Fill="#1F1F26" Stroke="#2B2B35" StrokeThickness="2"/>
        <Ellipse x:Name="LEye" Width="16" Height="16" Fill="{Binding EyeColor}" HorizontalAlignment="Left" Margin="95,62,0,0" VerticalAlignment="Top"/>
        <Ellipse x:Name="REye" Width="16" Height="16" Fill="{Binding EyeColor}" HorizontalAlignment="Right" Margin="0,62,95,0" VerticalAlignment="Top"/>
        <Rectangle x:Name="Mouth" Height="3" Width="52" RadiusX="2" RadiusY="2" Fill="{Binding MoodColor}" VerticalAlignment="Bottom" Margin="0,0,0,58" HorizontalAlignment="Center"/>
        <Rectangle x:Name="Lid" Height="0" Width="120" Fill="#1F1F26" VerticalAlignment="Top" Margin="0,74,0,0" HorizontalAlignment="Center"/>
    </Grid>
</UserControl>